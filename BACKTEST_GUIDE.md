# 回测执行指南

> 每次执行回测前必读，避免绕弯。

---

## 1. 唯一回测脚本

| 脚本 | 说明 |
|------|------|
| `run_backtest_5min_live_sim.py` | 核心回测引擎（5分钟K线），唯一有效脚本 |
| `run_v3_multiyear_backtest.py` | 多年批量包装脚本，调用上面的引擎 |

**其他所有回测脚本均已废弃**，不要使用。

---

## 2. 买入价模式（必看）

```
默认：close（5分钟K线收盘价）
```

| 模式 | 含义 | 适用场景 |
|------|------|---------|
| `close`（默认） | 以5分钟K线收盘价成交 | 标准回测，等价于实盘实时价 |
| `high` | 以5分钟K线最高价成交 | 不使用 |

**注意**：`close` 指5分钟K线自身的收盘价，不是日线收盘价。两者含义不同，不要混淆。

实盘采用实时价（卖一价限价单），5分钟bar收盘价是合理近似，故回测默认 `close`。

---

## 3. 年份数据目录配置

| 年份 | 5分钟数据目录 | 额外日线目录 | 说明 |
|------|-------------|------------|------|
| 2022 | `D:/5min_data_2022` | `D:/daily_data_2021_all` | 需要2021日线做lookback |
| 2023 | `D:/5min_data_2023` | 无 | |
| 2024 | `D:/5min_data_2024` | 无 | |
| 2025~2026 | `D:/5min_data`（默认） | 无 | |

日线数据（全市场）统一从 `D:/daily_data/SH` 和 `D:/daily_data/SZ` 加载，不需要指定。

---

## 4. 执行命令

### 跑所有年份（2022~2026）

```powershell
python -u run_v3_multiyear_backtest.py *> backtest_all_log.txt
```

### 跑指定年份

```powershell
# 单个年份
python -u run_v3_multiyear_backtest.py --years "2022" *> backtest_log.txt

# 多个年份（逗号分隔，注意 2025~2026 是一个标签）
python -u run_v3_multiyear_backtest.py --years "2022,2025~2026" *> backtest_log.txt
```

### 后台运行 + 轮询

```powershell
# 后台启动
python -u run_v3_multiyear_backtest.py --years "2022,2025~2026" *> backtest_log.txt &

# 每2分钟轮询（手动执行）
Get-Content d:\miniqmt_quant\backtest_log.txt -Tail 40
```

---

## 5. 最优参数（config.py）

| 参数 | 值 | 配置项 |
|------|-----|--------|
| 硬止损 | 3% | `V3_HARD_STOP_LOSS = 0.03` |
| 阴跌止损 | 2% | `V3_SOFT_STOP_LOSS = 0.02` |
| 移动止盈激活 | 2% | `V3_TRAILING_ACTIVATE = 0.02` |
| 移动止盈回撤 | 1% | `V3_TRAILING_STOP = 0.01` |
| 时间止损 | 5天 | `V3_TIME_STOP_DAYS = 5` |
| 防追高上限（主板） | 7% | `V3_MAX_CHANGE_PCT = 0.07` |
| 防追高上限（科创/创业） | 8% | `V3_STAR_MAX_CHANGE_PCT = 0.08` |
| 最大持仓数 | 3 | `V3_MAX_POSITIONS = 3` |
| 初始资金 | 30万 | `V3_INITIAL_CAPITAL = 300000` |

---

## 6. 选股池缓存机制

- 选股池会自动缓存到 `reports/pool_cache_{start}_{end}_{hash}.pkl`
- 缓存 key = `md5(start|end|fivemin_dir|extra_daily_dir)`
- 二次运行同参数**直接读缓存**，无需重算（约节省10分钟）
- 日线数据每次都必须重新加载（用于构建 `prev_close_cache` / `day_open_cache`）

---

## 7. 回测耗时参考

| 年份 | 数据加载 | 总耗时 |
|------|---------|--------|
| 2022 | ~18min | ~20min |
| 2023 | ~5min | ~7min |
| 2024 | ~5min | ~7min |
| 2025~2026 | ~8min | ~10min |

> 2022年因需额外加载2021日线（lookback），耗时最长。

---

## 8. 历史回测结果基准（close模式，最优参数）

| 年份 | 总收益 | 年化 | 最大回撤 | 夏普 | 胜率 | 盈亏比 | 笔数 |
|------|--------|------|---------|------|------|--------|------|
| 2022（熊市） | +217.45% | +229.81% | 14.87% | 3.439 | 52.58% | 1.39 | 561 |
| 2023 | -9.60% | -9.91% | 34.78% | -0.133 | 44.93% | 1.18 | 523 |
| 2024（牛市） | +86.61% | +90.50% | 20.63% | 1.909 | 51.57% | 1.21 | 543 |
| 2025~2026 | +2050.21% | +999.01% | 12.50% | 6.059 | 59.90% | 1.81 | 606 |

> **注意**：2023年结果为 `high` 模式跑出，与 `close` 模式结果可能有差异，待补跑确认。

---

## 9. 常见错误与避坑

| 错误 | 原因 | 解决 |
|------|------|------|
| 用了 `high` 模式跑回测 | 忘记默认值已改为 `close` | 检查日志第一行 `买入价模式` |
| 日志文件长期为0字节 | Python输出缓冲 | 启动时加 `-u` 参数 |
| 2022年跑很慢 | 需加载2021年额外日线 | 正常，等待即可 |
| 选股池重算而非读缓存 | 参数变化导致hash不匹配 | 正常，第一次跑完后有缓存 |
| 误用旧回测脚本 | 项目有多个历史回测脚本 | 只用 `run_backtest_5min_live_sim.py` |

---

## 10. 5分钟数据除权维护规则

### 背景

5分钟K线数据使用**前复权**价格（baostock `adjustflag='2'`）。

前复权的性质：
- 当天的前复权价 = 当天实际市价（正常情况下）
- 历史价格按除权因子向下调整，**买卖涨跌幅比例不变**，回测收益率准确
- 当股票发生**除权除息**（分红、送股、配股）后，该股票的所有历史K线需要重新调整

### 增量更新 vs 全量重下

| 操作 | 命令 | 修复除权历史调整 | 耗时 |
|------|------|----------------|------|
| 每日增量更新（自动） | `python update_5min_incremental.py` | ❌ 仅追加新日期 | ~5分钟 |
| 全量重下 | `python update_5min_incremental.py --force-full` | ✅ 全部重算 | ~1~2小时 |

> **重要**：增量更新只追加新日期，不会修正已有日期的价格。除权后数据偏差只能通过全量重下解决。

### 规则

1. **每日常规**：增量更新（自动化任务已配置，正常运行即可）
2. **每季度一次**（分红高峰期结束后）：手动执行全量重下，修正所有除权偏差
   - 建议时间节点：**5月下旬、8月下旬、11月下旬**（A股分红集中在4~7月、7~10月、10~12月）
3. **全量重下应在晚上21:00之后执行**（baostock当日除权数据通常在21:00前更新完毕）

```powershell
# 全量重下命令（建议睡前执行，约1~2小时）
cd d:\miniqmt_quant
python update_5min_incremental.py --force-full
```

### 对回测的影响

- **历史年份回测（2022~2024）**：不受影响，下载时已包含全部除权调整 ✓
- **近期数据（最近1~3个月）**：可能存在个别除权股票价格偏高，季度全量重下后消除
- **偏差幅度**：单只股票单次除权通常 0.5%~3%，对多股票组合回测影响极小
